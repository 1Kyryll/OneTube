import { UploadForm } from "../../features/video/components/UploadForm";

export default function UploadPage() {
    return (
        <main style={{ padding: 24 }}>
        <h1>Upload video</h1>
        <UploadForm />
        </main>
    );
}